# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Internal-only API URL (used in Server Components / API routes)
# Usually the same as the public one in dev
API_URL=http://localhost:8000
